# Retail Intelligence Backend — runtime dependencies.
# MongoDB is accessed through Motor (async PyMongo); there is no other database.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
motor>=3.6
pymongo>=4.9
pydantic>=2.7
python-dotenv>=1.0
websockets>=12.0
